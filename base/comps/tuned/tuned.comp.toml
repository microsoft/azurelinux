[components.tuned]
# Upstream Release: 1%{?prerel1}%{?git_suffix:.%{git_suffix}}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?prerel1\}%\{\?git_suffix:\.%\{git_suffix\}\}(?:%\{\??dist\})?$' } }
