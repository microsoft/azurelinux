[components.spandsp]
# Upstream Release: 21%{?pre:.pre%{pre}}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?pre:\.pre%\{pre\}\}(?:%\{\??dist\})?$' } }
