# Pin to 6.6.3 to match libplasma 6.6.3 SONAME (.so.7). The snapshot date
# (2026-02-24) picks 6.6.0 which links against libPlasma.so.6, but bootstrap
# builds linked against fc43's .so.7. Pinning keeps the cascade consistent.
[components.kde-cli-tools]
spec = { type = "upstream", upstream-distro = { name = "fedora", version = "43" }, upstream-commit = "358f6d3981c066b6f98dc3b970f2350cd4422958" }
