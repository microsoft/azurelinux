[components.tesseract-tessdata]
# Upstream Release: 11%{?pre:.%pre}%{?commit:.git%{shortcommit}}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?pre:\.%pre\}%\{\?commit:\.git%\{shortcommit\}\}(?:%\{\??dist\})?$' } }
