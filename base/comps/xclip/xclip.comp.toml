[components.xclip]
# Upstream Release: 25.git%{shortcommit}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)\.git%\{shortcommit\}(?:%\{\??dist\})?$' } }
