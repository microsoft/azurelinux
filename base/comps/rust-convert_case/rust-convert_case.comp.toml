[components.rust-convert_case]
# New component (no prior default existed). Pinned to Fedora f43 HEAD,
# providing convert_case 0.10.0, required by rust-proptest-macro (patched to
# accept crate(convert_case) >=0.10,<0.12). Older ranges 0.4/0.6/0.7 are
# served by rust-convert_case0.4/0.6/0.7 compat packages; 0.8.x/0.9.x have no
# provider today — add a compat package if a consumer needs that range.
spec = { type = "upstream", upstream-distro = { name = "fedora", version = "43" }, upstream-commit = "cdfa9545236b7c8ff0e311ccab27c8a364d8127a" }
