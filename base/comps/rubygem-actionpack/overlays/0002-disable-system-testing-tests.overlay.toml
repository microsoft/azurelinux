[[overlays]]
description = "Disable system_testing tests - all instantiate chrome drivers and fail without chromedriver"
type = "spec-search-replace"
regex = "# Tests need to run in isolation"
replacement = "# Azure Linux: no chromedriver available, so skip all system_testing tests\n# (driver_test.rb, system_test_case_test.rb, screenshot_helper_test.rb,\n# server_test.rb all instantiate Selenium drivers).\nrm -rf test/dispatch/system_testing\n\n# Tests need to run in isolation"
