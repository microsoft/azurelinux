[components.qt-mobility]
# Upstream Release: 0.50.%{snap}git%{git_short}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^0\.([0-9]+)\.%\{snap\}git%\{git_short\}(?:%\{\??dist\})?$' } }
