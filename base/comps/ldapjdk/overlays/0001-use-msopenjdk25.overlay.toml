[metadata]
category = "azl-compatibility"
upstream-status = "inapplicable"

[[overlays]]
description = "Use Microsoft OpenJDK 25 for LDAP JDK build dependencies"
type = "spec-search-replace"
regex = '^%define java_devel java-25-openjdk-devel$'
replacement = '%define java_devel msopenjdk-25'

[[overlays]]
description = "Use Microsoft OpenJDK 25 for LDAP JDK runtime dependencies"
type = "spec-search-replace"
regex = '^%define java_headless java-25-openjdk-headless$'
replacement = '%define java_headless msopenjdk-25'

[[overlays]]
description = "Use the Microsoft OpenJDK 25 installation as LDAP JDK JAVA_HOME"
type = "spec-search-replace"
regex = '^%define java_home %\{_jvmdir\}/jre-25-openjdk$'
replacement = '%define java_home %{_jvmdir}/msopenjdk-25'