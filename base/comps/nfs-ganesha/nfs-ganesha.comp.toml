[components.nfs-ganesha]
# Upstream Release: 2%{?dev:%{dev}}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?dev:%\{dev\}\}(?:%\{\??dist\})?$' } }
