# Azure Linux runs on Azure, where the Azure datasource always applies.
# Without an explicit list cloud-init probes every supported provider on each
# boot, which measurably delays cloud-init-local and therefore network-pre.
#
# Mirrors the list Azure Linux 3.0 shipped in /etc/cloud/cloud.cfg.
datasource_list: [ Azure, NoCloud, None ]
