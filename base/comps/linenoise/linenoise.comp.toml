[components.linenoise]
# Upstream Release: 13.20200312git%{shortcommit}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)\.20200312git%\{shortcommit\}(?:%\{\??dist\})?$' } }
