[components.libldm]
# Upstream Release: 2%{?dist}%{?extra_release}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)(?:%\{\??dist\})?%\{\?extra_release\}$' } }
