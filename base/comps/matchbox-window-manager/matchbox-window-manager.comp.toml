[components.matchbox-window-manager]
# Upstream Release: 39.%{alphatag}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)\.%\{alphatag\}(?:%\{\??dist\})?$' } }
