[components.rust-databake-derive]
# Pin to the f43 commit that ships databake-derive 0.2.1 (adds the public-field
# assertion). Keep in lockstep with rust-databake so the lib's doctests match.
# TODO: Drop this override once the default Fedora 43 snapshot
# in distro/azurelinux.distro.toml advances past this commit.
spec = { type = "upstream", upstream-name = "rust-databake-derive", upstream-distro = { name = "fedora", version = "43" }, upstream-commit = "fa79cdf487db420a19e1b2818ad02dde0e4b69f2" }
