[[overlays]]
description = "Drop BuildRequires on cmake(Notcurses++) — only used by default-OFF DEVTOOL_REPOMIRROR; notcurses being removed"
type = "spec-remove-tag"
tag = "BuildRequires"
value = "cmake(Notcurses++)"
