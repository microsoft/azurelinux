[components.postgresql18-decoderbufs]
# Upstream Release: 2%{?pre:.%pre}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?pre:\.%pre\}(?:%\{\??dist\})?$' } }
