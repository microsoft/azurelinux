[components.tpm2-tools]
# Upstream Release: 4%{?candidate:.%{candidate}}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?candidate:\.%\{candidate\}\}(?:%\{\??dist\})?$' } }
