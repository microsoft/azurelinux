[components.zaf]
# Upstream Release: 0.32.%{upstreamid}svn%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^0\.([0-9]+)\.%\{upstreamid\}svn(?:%\{\??dist\})?$' } }
