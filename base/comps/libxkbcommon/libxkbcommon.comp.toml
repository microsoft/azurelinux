[components.libxkbcommon]
# Upstream Release: 1%{?gitdate:.%{gitdate}}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?gitdate:\.%\{gitdate\}\}(?:%\{\??dist\})?$' } }
