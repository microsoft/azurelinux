# Azure Linux HWE aarch64 configuration overrides.
#
# Add only settings that intentionally differ from the standard Azure Linux
# kernel config. The shared baseline is applied first, then this file.
