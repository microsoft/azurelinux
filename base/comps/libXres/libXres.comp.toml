[components.libXres]
# Upstream Release: 6%{?gitdate:.%{gitdate}git%{gitversion}}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?gitdate:\.%\{gitdate\}git%\{gitversion\}\}(?:%\{\??dist\})?$' } }
