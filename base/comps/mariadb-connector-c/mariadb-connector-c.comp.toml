[components.mariadb-connector-c]
# Upstream Release: 3%{?with_debug:.debug}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?with_debug:\.debug\}(?:%\{\??dist\})?$' } }
