[components.kernel]
# Azure Linux maintains the kernel as a local spec (migrated away from the
# azldev TOML-overlay-on-Fedora-upstream approach). The spec and all of its
# source files live alongside this file in base/comps/kernel/. Edit
# kernel.spec directly; AZL-specific changes are marked with "AZL:" comments.
spec = { type = "local", path = "kernel.spec" }

# The spec uses %{specrelease} (kextraversion.azl_pkgrelease.dist) which the
# auto-calculator cannot parse. Release is managed manually: bump azl_pkgrelease
# in kernel.spec when rebuilding without a version change. The %changelog is
# hand-curated inline in the spec.
release = { calculation = "manual" }

# --- Downloaded source tarballs (not vendored into the component dir) ---

# Azure Linux kernel source tarball (rolling-lts/azl4).
[[components.kernel.source-files]]
filename = "kernel-6.18.31.1.tar.gz"
hash = "1bf684812ff3fc38974dc99b3e2309cb46dc5af93ac794e79a4a4f3dd17debfadb14510aeaaa7fa2d4c6254876a9f8bc67b900a1951c8dda5d94e5f5028a7557"
hash-type = "SHA512"
origin = { type = "download", uri = "https://github.com/microsoft/CBL-Mariner-Linux-Kernel/archive/rolling-lts/azl4/6.18.31.1.tar.gz" }

# NVIDIA open GPU kernel module source tarball (kmod-nvidia-open subpackage).
[[components.kernel.source-files]]
filename = "open-gpu-kernel-modules-595.58.03.tar.gz"
hash = "a422b6935209d590f57fa6766f59bb207d9130f8a6777af9245c4ff8cd0f4c4ccef4602a0b26d543e1e8efba24241180992fdb749ea4e5d2aa5218a584b85101"
hash-type = "SHA512"
origin = { type = "download", uri = "https://github.com/NVIDIA/open-gpu-kernel-modules/archive/refs/tags/595.58.03.tar.gz" }

# Disabled inherited kABI checks do not require reference source tarballs.
