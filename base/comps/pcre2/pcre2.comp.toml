[components.pcre2]
# Upstream Release: %{?rcversion:0.}1%{?rcversion:.%rcversion}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^%\{\?rcversion:0\.\}([0-9]+)%\{\?rcversion:\.%rcversion\}(?:%\{\??dist\})?$' } }
