[metadata]
category = "azl-compatibility"
upstream-status = "upstreamable"

[[overlays]]
description = "Format cookie MaxAge as an integer for Go 1.26 vet"
type = "patch-add"
source = "../0001-tests-format-cookie-max-age-as-integer.patch"

[[overlays]]
description = "Apply the Go 1.26 format validation patch"
type = "spec-append-lines"
section = "%prep"
lines = ["%autopatch -p1"]