[components.urlview]
# Upstream Release: 40.%{gitdate}git%{gitrev}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)\.%\{gitdate\}git%\{gitrev\}(?:%\{\??dist\})?$' } }
