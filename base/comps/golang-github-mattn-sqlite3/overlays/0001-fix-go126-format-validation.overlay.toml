[metadata]
category = "azl-compatibility"
upstream-status = "upstreamable"

[[overlays]]
description = "Format sqlite row counts as integers for Go 1.26 vet"
type = "patch-add"
source = "../0001-tests-format-row-counts-as-integers.patch"

[[overlays]]
description = "Apply the Go 1.26 format validation patch"
type = "spec-append-lines"
section = "%prep"
lines = ["%autopatch -p1"]
