[components.mariadb-connector-odbc]

[[components.mariadb-connector-odbc.overlays]]
description = "Switch Release to %autorelease while keeping the with_debug suffix before dist"
type = "spec-set-tag"
tag = "Release"
value = "%autorelease %{?with_debug:-e debug}"
