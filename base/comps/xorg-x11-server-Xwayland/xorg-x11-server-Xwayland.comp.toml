[components.xorg-x11-server-Xwayland]
# Upstream Release: 2%{?gitdate:.%{gitdate}git%{shortcommit}}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?gitdate:\.%\{gitdate\}git%\{shortcommit\}\}(?:%\{\??dist\})?$' } }
