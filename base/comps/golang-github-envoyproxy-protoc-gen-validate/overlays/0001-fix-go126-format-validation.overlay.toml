[metadata]
category = "azl-compatibility"
upstream-status = "upstreamable"

[[overlays]]
description = "Pass the undefined enum value to Failf for Go 1.26 vet"
type = "patch-add"
source = "../0001-checker-pass-undefined-enum-value.patch"

[[overlays]]
description = "Apply the Go 1.26 format validation patch"
type = "spec-append-lines"
section = "%prep"
lines = ["%autopatch -p1"]
