[components.lirc]
# Upstream Release: 4%{?tag:.}%{?tag}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?tag:\.\}%\{\?tag\}(?:%\{\??dist\})?$' } }
