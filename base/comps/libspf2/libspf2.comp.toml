[components.libspf2]
# Upstream Release: 19.20210922git%{git}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)\.20210922git%\{git\}(?:%\{\??dist\})?$' } }
