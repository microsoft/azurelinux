[components.php-phpdocumentor-reflection-common2]
# Upstream Release: 15%{?github_release}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)%\{\?github_release\}(?:%\{\??dist\})?$' } }
