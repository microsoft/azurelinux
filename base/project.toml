#
# Base project
#

includes = ["comps/components.toml", "images/images.toml", "packages/*packages.toml"]

[project]
description = 'azurelinux-base'
log-dir = 'build/logs'
work-dir = 'build/work'
output-dir = 'out'
default-distro = { name = "azurelinux" }
