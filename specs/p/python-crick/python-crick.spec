## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 9;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

%global pypi_name crick

Name:           python-%{pypi_name}
Version:        0.0.8
Release:        %{autorelease}
Summary:        High performance approximate and streaming algorithms

%global forgeurl https://github.com/dask/crick
%global tag %{version}
%forgemeta

# Apache-2.0 applies to `crick/tdigest_stubs.c`
# MIT applies to `crick/klib/khash.h`
License:        BSD-3-Clause AND Apache-2.0 AND MIT
URL:            %forgeurl
Source:         %forgesource
# Fix a segfault in the test suite
Patch:          %{forgeurl}/pull/69.patch

ExcludeArch:    %{ix86}
BuildRequires:  gcc
BuildRequires:  python3-devel
# Test dependencies
BuildRequires:  python3dist(pytest)
BuildRequires:  python3dist(scipy)

%global _description %{expand:
Crick is a fast library of approximate and streaming algorithms. It is
still a work in progress, use at your own risk.}

%description %_description


%package -n python3-%{pypi_name}
Summary:        %{summary}

%description -n python3-%{pypi_name} %_description


%prep
%forgeautosetup -p1

# Don't install devel (.c, .h) files
# Also exclude test dir
# Sent upstream: https://github.com/dask/crick/pull/52
sed -r \
    -e 's/(include-package-data = )true/\1false/' \
    -e '/namespaces/a exclude = ["crick.tests*"]' \
    -i pyproject.toml

# Allow building with NumPy 1.x. Required for Fedora < 42.
sed -r \
    -e 's/(numpy)[>=]*2.*"/\1"/' \
    -i pyproject.toml


%generate_buildrequires
%pyproject_buildrequires


%build
%pyproject_wheel


%install
%pyproject_install
%pyproject_save_files -l %{pypi_name}


%check
# Ugly hack to make the tests work.
# Upstream uses an editable build, which drops the .so files in the tree.
# The usual remedies like using `--import-mode=importlib` do not work.
pushd crick
ln -s %{buildroot}%{python3_sitearch}/crick/*.so .
popd
%pytest -v


%files -n python3-%{pypi_name} -f %{pyproject_files}
%doc README.rst
%license EXTERNAL_LICENSES


%changelog
## START: Generated by rpmautospec
* Wed Apr 22 2026 azldev <azurelinux@microsoft.com> - 0.0.8-9
- Latest state for python-crick

* Fri Sep 19 2025 Python Maint <python-maint@redhat.com> - 0.0.8-8
- Rebuilt for Python 3.14.0rc3 bytecode

* Fri Aug 15 2025 Python Maint <python-maint@redhat.com> - 0.0.8-7
- Rebuilt for Python 3.14.0rc2 bytecode

* Wed Jul 30 2025 Jerry James <loganjerry@gmail.com> - 0.0.8-6
- Fix reference counting issue
- Add MIT to License due to khash.h

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.0.8-5
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Tue Jun 03 2025 Python Maint <python-maint@redhat.com> - 0.0.8-4
- Rebuilt for Python 3.14

* Mon Jan 20 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.0.8-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_42_Mass_Rebuild

* Mon Dec 23 2024 Packit <hello@packit.dev> - 0.0.8-1
- Update to 0.0.8 upstream release
- Resolves: rhbz#2333866

* Mon Dec 16 2024 Packit <hello@packit.dev> - 0.0.7-1
- Update to 0.0.7 upstream release
- Resolves: rhbz#2306451

* Tue Oct 29 2024 Sandro <devel@penguinpee.nl> - 0.0.6-3
- Fix module import failing

* Fri Jul 19 2024 Fedora Release Engineering <releng@fedoraproject.org> - 0.0.6-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_41_Mass_Rebuild

* Fri Jun 28 2024 Sandro <devel@penguinpee.nl> - 0.0.6-1
- Initial release (RHBZ#2293043)
## END: Generated by rpmautospec
