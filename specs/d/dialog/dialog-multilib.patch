diff -up dialog-1.3-20250116/dialog-config.in.multilib dialog-1.3-20250116/dialog-config.in
--- dialog-1.3-20250116/dialog-config.in.multilib	2025-01-16 22:29:50.000000000 +0100
+++ dialog-1.3-20250116/dialog-config.in	2025-03-03 11:40:58.831756256 +0100
@@ -57,7 +57,7 @@ while [ $# -gt 0 ]; do
 	fi
 
 	eval bindir='"@bindir@"'
-	eval libdir='"@libdir@"'
+	eval libdir=''
 	eval datadir='"@datadir@"'
 	eval includedir='"@includedir@"'
 	eval mandir='"@mandir@"'
@@ -70,8 +70,8 @@ while [ $# -gt 0 ]; do
 	need_subinclude=
 	[ "@SUB_INC@" = yes ] && need_subinclude="-I$includedir/$THIS"
 
-	eval LDFLAGS='"@LDFLAGS@"'
-	eval LIBS='"@LIBS@"'
+	eval LDFLAGS=''
+	eval LIBS=''
 	LIBS="-l${THIS} $LIBS"
 
 	# If the directory given by --libdir is not in the LDFLAGS+LIBS set,
@@ -85,12 +85,12 @@ while [ $# -gt 0 ]; do
 			break
 		fi
 	done
-	if [ $found = no ]
+	if false
 	then
 		[ -n "$LFLAGS" ] && LDFLAGS=" $LFDLAGS"
 		LDFLAGS="-L${libdir}$LDFLAGS"
 	fi
-	[ -z "$LDFLAGS" ] && LDFLAGS="-L${exec_prefix}/lib"
+	#[ -z "$LDFLAGS" ] && LDFLAGS="-L${exec_prefix}/lib"
 
 	# Ignore -L options which do not correspond to an actual directory,
 	# or which are standard library directories (i.e., the linker is
@@ -118,41 +118,6 @@ while [ $# -gt 0 ]; do
 			lib_check=`echo "x$opt" | sed -e 's/^.-L//'`
 			[ -d "$lib_check" ] || continue
 			case "$lib_check" in
-			@LD_SEARCHPATH@) # skip standard libdir
-				if [ "$lib_check" = "$libdir" ]
-				then
-					lib_first=yes
-					IFS_save="$IFS"
-					IFS='|'
-					LIBDIRS="@LD_SEARCHPATH@"
-					for lib_check in $LIBDIRS
-					do
-						if [ -d "$lib_check" ]
-						then
-							if [ "$lib_check" != "$libdir" ]
-							then
-								lib_first=no
-							fi
-							break
-						fi
-					done
-					IFS="$IFS_save"
-					[ $lib_first = yes ] && continue
-					found=no
-					for check in $lib_flags
-					do
-						if [ "x$check" = "x$opt" ]
-						then
-							found=yes
-							break
-						fi
-					done
-					[ $found = yes ] && continue
-					:
-				else
-					continue
-				fi
-				;;
 			*)
 				found=no
 				for check in $lib_flags
