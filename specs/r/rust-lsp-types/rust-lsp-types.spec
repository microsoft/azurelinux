## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 6;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate lsp-types

Name:           rust-lsp-types
Version:        0.95.1
Release:        %autorelease
Summary:        Types for interaction with a language server

License:        MIT
URL:            https://crates.io/crates/lsp-types
Source:         %{crates_source}
Patch:          0001-Drop-an-invalidly-formatted-code-block-from-document.patch

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  dos2unix

%global _description %{expand:
Types for interaction with a language server, using VSCode's Language
Server Protocol.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%exclude %{crate_instdir}/release.sh
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+proposed-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+proposed-devel %{_description}

This package contains library source intended for building other packages which
use the "proposed" feature of the "%{crate}" crate.

%files       -n %{name}+proposed-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
# Fix EOL encodings
for f in CHANGELOG.md README.md; do
    # Keep timestamp of the old file
    touch -r $f $f.tstamp
    dos2unix $f
    touch -r $f.tstamp $f
    rm $f.tstamp
done
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Wed Apr 22 2026 azldev <azurelinux@microsoft.com> - 0.95.1-6
- Latest state for rust-lsp-types

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.95.1-5
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Sun Jan 19 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.95.1-4
- Rebuilt for https://fedoraproject.org/wiki/Fedora_42_Mass_Rebuild

* Thu Aug 08 2024 Fabio Valentini <decathorpe@gmail.com> - 0.95.1-3
- Drop an invalidly formatted code block from documentation

* Sat Jul 20 2024 Fedora Release Engineering <releng@fedoraproject.org> - 0.95.1-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_41_Mass_Rebuild

* Fri Apr 05 2024 blinxen <h-k-81@hotmail.com> - 0.95.1-1
- Update to version 0.95.1 (rhbz#2254195)

* Fri Jan 26 2024 Fedora Release Engineering <releng@fedoraproject.org> - 0.94.1-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_40_Mass_Rebuild

* Sun Oct 08 2023 blinxen <h-k-81@hotmail.com> - 0.94.1-1
- Initial package (rhbz#2242623)
## END: Generated by rpmautospec
