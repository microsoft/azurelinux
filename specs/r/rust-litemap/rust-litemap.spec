## START: Set by rpmautospec
## (rpmautospec version 0.8.3)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 4;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate litemap

Name:           rust-litemap
Version:        0.8.1
Release:        %autorelease
Summary:        Key-value Map implementation based on a flat, sorted Vec

License:        Unicode-3.0
URL:            https://crates.io/crates/litemap
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Drop benchmark-only criterion dependency
# * Omit the language_names_hash_map and language_names_lite_map examples; they
#   would require icu_locale_core (circularly) and the internal
#   icu_benchmark_macros crate
Patch:          litemap-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A key-value Map implementation based on a flat, sorted Vec.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+alloc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+alloc-devel %{_description}

This package contains library source intended for building other packages which
use the "alloc" feature of the "%{crate}" crate.

%files       -n %{name}+alloc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+databake-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+databake-devel %{_description}

This package contains library source intended for building other packages which
use the "databake" feature of the "%{crate}" crate.

%files       -n %{name}+databake-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+testing-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+testing-devel %{_description}

This package contains library source intended for building other packages which
use the "testing" feature of the "%{crate}" crate.

%files       -n %{name}+testing-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+yoke-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+yoke-devel %{_description}

This package contains library source intended for building other packages which
use the "yoke" feature of the "%{crate}" crate.

%files       -n %{name}+yoke-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%if %{defined el9}
# * EPEL9: These tests require a newer Rust than the MSRV
%{cargo_test -- -- %{shrink:
    --skip const_get_indexed_or_panic
    --skip const_get_with_index
    --skip const_len
}}
%else
%cargo_test
%endif
%endif

%changelog
## START: Generated by rpmautospec
* Thu Apr 30 2026 Daniel McIlvaney <damcilva@microsoft.com> - 0.8.1-4
- test: add initial lock files

* Sat Jan 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 0.8.1-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_44_Mass_Rebuild

* Thu Oct 30 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.8.1-2
- On EPEL9, skip a few tests that need newer Rust

* Wed Oct 29 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.8.1-1
- Update to version 0.8.1; Fixes RHBZ#2406874

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.8.0-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Wed May 21 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.8.0-1
- Update to version 0.8.0; Fixes RHBZ#2364947

* Wed May 21 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.7.3-7
- Restore postcard dev-dependency

* Mon May 12 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.7.3-6
- Remove no-longer-necessary .rpmlintrc file

* Mon Apr 07 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.7.3-5
- Stop packaging the bench feature, only relevant for CI

* Sat Mar 15 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.7.3-4
- Fix test panics/segfaults on big-endian hosts

* Fri Mar 14 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.7.3-3
- Report and skip s390x test failures
- These appear to be testing issues, not implementation bugs.

* Fri Mar 14 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.7.3-1
- Initial package (close RHBZ#2328546)
## END: Generated by rpmautospec
