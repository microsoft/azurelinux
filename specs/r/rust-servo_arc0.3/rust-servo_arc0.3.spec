## START: Set by rpmautospec
## (rpmautospec version 0.8.3)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 2;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate servo_arc

Name:           rust-servo_arc0.3
Version:        0.3.0
Release:        %autorelease
Summary:        Fork of std::sync::Arc with some extra functionality and without weak references

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/servo_arc
Source:         %{crates_source}
# Add license files from upstream git repo as they seem to have gone missing in the 0.3.0 crate
# https://github.com/servo/servo/issues/30455
Source1:        https://raw.githubusercontent.com/servo/servo/master/components/servo_arc/LICENSE-APACHE
Source2:        https://raw.githubusercontent.com/servo/servo/master/components/servo_arc/LICENSE-MIT

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A fork of std::sync::Arc with some extra functionality and without weak
references.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+gecko_refcount_logging-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+gecko_refcount_logging-devel %{_description}

This package contains library source intended for building other packages which
use the "gecko_refcount_logging" feature of the "%{crate}" crate.

%files       -n %{name}+gecko_refcount_logging-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+servo-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+servo-devel %{_description}

This package contains library source intended for building other packages which
use the "servo" feature of the "%{crate}" crate.

%files       -n %{name}+servo-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp %{SOURCE1} .
cp %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Thu Apr 30 2026 Daniel McIlvaney <damcilva@microsoft.com> - 0.3.0-2
- test: add initial lock files

* Thu Mar 05 2026 Fabio Valentini <decathorpe@gmail.com> - 0.3.0-1
- Initial import (servo_arc v0.3 compat package)
## END: Generated by rpmautospec
