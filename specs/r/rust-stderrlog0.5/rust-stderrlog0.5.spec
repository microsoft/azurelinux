## START: Set by rpmautospec
## (rpmautospec version 0.8.3)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 5;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate stderrlog

Name:           rust-stderrlog0.5
Version:        0.5.4
Release:        %autorelease
Summary:        Logger that logs to stderr based on verbosity specified

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/stderrlog
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * relax termcolor dependency from ~1.1 to ^1.1
# * relax thread_local dependency from ~1.1 to ^1.1
# * drop WASM-specific chrono/wasmbind dependency
# * drop dev-dependencies that are present only for example code
Patch:          stderrlog-fix-metadata.diff
Patch:          0001-skip-doctests-that-pull-in-additional-dependencies.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Logger that logs to stderr based on verbosity specified.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/SECURITY.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+chrono-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+chrono-devel %{_description}

This package contains library source intended for building other packages which
use the "chrono" feature of the "%{crate}" crate.

%files       -n %{name}+chrono-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+timestamps-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+timestamps-devel %{_description}

This package contains library source intended for building other packages which
use the "timestamps" feature of the "%{crate}" crate.

%files       -n %{name}+timestamps-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# drop example code that pulls in many additional dependencies
rm -rf examples/

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Thu Apr 30 2026 Daniel McIlvaney <damcilva@microsoft.com> - 0.5.4-5
- test: add initial lock files

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.5.4-4
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Sat Jan 18 2025 Fabio Valentini <decathorpe@gmail.com> - 0.5.4-3
- Drop examples and skip doctests which pull in additional dependencies

* Sat Jul 20 2024 Fedora Release Engineering <releng@fedoraproject.org> - 0.5.4-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_41_Mass_Rebuild

* Thu Jul 11 2024 Fabio Valentini <decathorpe@gmail.com> - 0.5.4-1
- Initial import (stderrlog 0.5 compat package)
## END: Generated by rpmautospec
