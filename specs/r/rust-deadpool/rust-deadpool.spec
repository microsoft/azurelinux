## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 3;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate deadpool

Name:           rust-deadpool
Version:        0.13.0
Release:        %autorelease
Summary:        Dead simple async pool

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/deadpool
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Omit benchmark-only criterion dev-dependency
# * Omit deprecated async-std dev-dependency
# * Omit config dev-dependency; it is required only for a single test, which is
#   not even compiled unless we build and run tests with both the managed and
#   serde features. (Only managed is a default feature.)
# * Omit smol-macros dev-dependency; there is currently no packaged
#   rust-smol-macros, and we do not want to package it just for a single test. A
#   small source-doc patch avoids compiling the test in question.
# * Exclude the test-build.sh shell script to avoid generating an
#   otherwise-unnecessary dependency on the bash shell.
Patch:          deadpool-fix-metadata.diff
# * Do not compile the test that would require smol-macros.
Patch10:        deadpool-0.13.0-no-smol-macros.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Dead simple async pool.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+managed-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+managed-devel %{_description}

This package contains library source intended for building other packages which
use the "managed" feature of the "%{crate}" crate.

%files       -n %{name}+managed-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rt_smol_2-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rt_smol_2-devel %{_description}

This package contains library source intended for building other packages which
use the "rt_smol_2" feature of the "%{crate}" crate.

%files       -n %{name}+rt_smol_2-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rt_tokio_1-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rt_tokio_1-devel %{_description}

This package contains library source intended for building other packages which
use the "rt_tokio_1" feature of the "%{crate}" crate.

%files       -n %{name}+rt_tokio_1-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+unmanaged-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unmanaged-devel %{_description}

This package contains library source intended for building other packages which
use the "unmanaged" feature of the "%{crate}" crate.

%files       -n %{name}+unmanaged-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Mon Apr 06 2026 azldev <> - 0.13.0-3
- Latest state for rust-deadpool

* Mon Feb 23 2026 Benjamin A. Beasley <code@musicinmybrain.net> - 0.13.0-2
- Don’t mark README.md as documentation since it’s used in compiling

* Wed Feb 18 2026 Benjamin A. Beasley <code@musicinmybrain.net> - 0.13.0-1
- Update to version 0.13.0; Fixes RHBZ#2440400

* Sat Jan 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 0.12.3-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_44_Mass_Rebuild

* Tue Sep 02 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.12.3-1
- Update to version 0.12.3; Fixes RHBZ#2392530

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.12.2-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Tue May 13 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.12.2-1
- Initial package (close RHBZ#2365475)
## END: Generated by rpmautospec
