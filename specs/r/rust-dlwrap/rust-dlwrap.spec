## START: Set by rpmautospec
## (rpmautospec version 0.8.3)
## RPMAUTOSPEC: autochangelog
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1

%global crate dlwrap

Name:           rust-dlwrap
Version:        0.3.9
Release: 3%{?dist}
Summary:        Dlopen wrapper code generator

License:        Apache-2.0
URL:            https://crates.io/crates/dlwrap
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Dlopen wrapper code generator.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# (MIT OR Apache-2.0) AND Unicode-DFS-2016
# Apache-2.0
# Apache-2.0 OR MIT
# ISC
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
License:        %{shrink:
    Apache-2.0 AND
    ISC AND
    MIT AND
    Unicode-DFS-2016 AND
    (Apache-2.0 OR MIT) AND
    (Unlicense OR MIT)
}
# LICENSE.dependencies contains a full license breakdown
Requires:       clang-libs

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_bindir}/dlwrap

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+clang-runtime-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+clang-runtime-devel %{_description}

This package contains library source intended for building other packages which
use the "clang-runtime" feature of the "%{crate}" crate.

%files       -n %{name}+clang-runtime-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -f clang-runtime

%build
%cargo_build -f clang-runtime
%{cargo_license_summary -f clang-runtime}
%{cargo_license -f clang-runtime} > LICENSE.dependencies

%install
%cargo_install -f clang-runtime

%if %{with check}
%check
%cargo_test -f clang-runtime
%endif

%changelog
## START: Generated by rpmautospec
* Thu Apr 30 2026 Daniel McIlvaney <damcilva@microsoft.com> - 0.3.9-2
- test: add initial lock files

* Mon Jan 12 2026 Fabio Valentini <decathorpe@gmail.com> - 0.3.9-1
- Update to version 0.3.9; Fixes RHBZ#2415794

* Fri Jul 25 2025 Daiki Ueno <dueno@redhat.com> - 0.3.7-1
- Initial import
## END: Generated by rpmautospec
