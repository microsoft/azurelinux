## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 7;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate datatest-stable

Name:           rust-datatest-stable
Version:        0.3.3
Release:        %autorelease
Summary:        Data-driven tests that work on stable Rust

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/datatest-stable
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Allow all fancy-regex releases between 0.13 and 0.17, inclusive: “Update
#   fancy-regex dependency to 0.17”
#   https://github.com/nextest-rs/datatest-stable/pull/450
# * Exclude some unnecessary files from the crate:
#   https://github.com/nextest-rs/datatest-stable/pull/424. Excluding scripts/
#   in particular avoids an unwanted dependency on bash.
Patch:          datatest-stable-fix-metadata.diff
# * Update trybuild output for ui test for Rust 1.93
Patch10:        https://github.com/nextest-rs/datatest-stable/pull/449.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Data-driven tests that work on stable Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+include-dir-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+include-dir-devel %{_description}

This package contains library source intended for building other packages which
use the "include-dir" feature of the "%{crate}" crate.

%files       -n %{name}+include-dir-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -f include-dir

%build
%cargo_build -f include-dir

%install
%cargo_install -f include-dir

%if %{with check}
%check
# * Tests with run_example in their names require nextest (rust-cargo-nextest),
#   which is not packaged.
%{cargo_test -f include-dir -- -- --exact %{shrink:
    --skip run_example
    --skip unix::run_example_with_colons
}}
%endif

%changelog
## START: Generated by rpmautospec
* Wed Apr 22 2026 azldev <azurelinux@microsoft.com> - 0.3.3-7
- Latest state for rust-datatest-stable

* Tue Feb 03 2026 Benjamin A. Beasley <code@musicinmybrain.net> - 0.3.3-6
- Allow fancy-regex through 0.17

* Tue Feb 03 2026 Benjamin A. Beasley <code@musicinmybrain.net> - 0.3.3-4
- Update trybuild output for ui test for Rust 1.93

* Sat Jan 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 0.3.3-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_44_Mass_Rebuild

* Sat Dec 27 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.3.3-2
- Exclude some unnecessary files from the crate
- Fixes an unwanted dependency on bash

* Sat Dec 27 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.3.3-1
- Initial package (close RHBZ#2423802)
## END: Generated by rpmautospec
