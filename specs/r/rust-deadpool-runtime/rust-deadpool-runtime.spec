## START: Set by rpmautospec
## (rpmautospec version 0.8.3)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 3;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate deadpool-runtime

Name:           rust-deadpool-runtime
Version:        0.3.1
Release:        %autorelease
Summary:        Dead simple async pool utilities for async runtimes

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/deadpool-runtime
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Dead simple async pool utilities for async runtimes.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+smol_2-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+smol_2-devel %{_description}

This package contains library source intended for building other packages which
use the "smol_2" feature of the "%{crate}" crate.

%files       -n %{name}+smol_2-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+tokio_1-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+tokio_1-devel %{_description}

This package contains library source intended for building other packages which
use the "tokio_1" feature of the "%{crate}" crate.

%files       -n %{name}+tokio_1-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Thu Apr 30 2026 Daniel McIlvaney <damcilva@microsoft.com> - 0.3.1-3
- test: add initial lock files

* Mon Feb 23 2026 Benjamin A. Beasley <code@musicinmybrain.net> - 0.3.1-2
- Don’t mark README.md as documentation since it’s used in compiling

* Wed Feb 18 2026 Benjamin A. Beasley <code@musicinmybrain.net> - 0.3.1-1
- Update to version 0.3.1; Fixes RHBZ#2436073

* Sat Jan 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 0.1.4-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_44_Mass_Rebuild

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.1.4-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Mon May 12 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.1.4-1
- Initial package (close RHBZ#2365474)
## END: Generated by rpmautospec
