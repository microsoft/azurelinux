## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 8;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate cargo_metadata

Name:           rust-cargo_metadata0.15
Version:        0.15.4
Release:        %autorelease
Summary:        Structured access to the output of cargo metadata

License:        MIT
URL:            https://crates.io/crates/cargo_metadata
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump derive_builder dependency from 0.11.1 to 0.20
Patch:          cargo_metadata-fix-metadata.diff

# upstream patch to adapt tests for new error message format with Rust 1.91+
Patch:          https://github.com/oli-obk/cargo_metadata/commit/aae754b.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Structured access to the output of `cargo metadata`.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+builder-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+builder-devel %{_description}

This package contains library source intended for building other packages which
use the "builder" feature of the "%{crate}" crate.

%files       -n %{name}+builder-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+derive_builder-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+derive_builder-devel %{_description}

This package contains library source intended for building other packages which
use the "derive_builder" feature of the "%{crate}" crate.

%files       -n %{name}+derive_builder-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -a

%build
%cargo_build -a

%install
%cargo_install -a

%if %{with check}
%check
# * skip tests that depend on fixtures which are not included in published crates
%cargo_test -a -- -- --skip advanced_feature_configuration --skip all_the_fields --skip basic_workspace_root_package_exists --skip current_dir
%endif

%changelog
## START: Generated by rpmautospec
* Mon Apr 06 2026 azldev <> - 0.15.4-8
- Latest state for rust-cargo_metadata0.15

* Thu Nov 27 2025 Fabio Valentini <decathorpe@gmail.com> - 0.15.4-7
- Include upstream patch to fix tests with Rust 1.91+

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.15.4-6
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Sun Jan 19 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.15.4-5
- Rebuilt for https://fedoraproject.org/wiki/Fedora_42_Mass_Rebuild

* Sun Oct 06 2024 Fabio Valentini <decathorpe@gmail.com> - 0.15.4-4
- Bump derive_builder dependency from 0.12 to 0.20

* Fri Jul 19 2024 Fedora Release Engineering <releng@fedoraproject.org> - 0.15.4-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_41_Mass_Rebuild

* Fri Jan 26 2024 Fedora Release Engineering <releng@fedoraproject.org> - 0.15.4-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_40_Mass_Rebuild

* Tue Sep 05 2023 Fabio Valentini <decathorpe@gmail.com> - 0.15.4-1
- Initial import (cargo_metadata 0.15 compat package)
## END: Generated by rpmautospec
