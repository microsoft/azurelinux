## START: Set by rpmautospec
## (rpmautospec version 0.8.3)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 2;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate zerotrie

Name:           rust-zerotrie
Version:        0.2.5
Release:        %autorelease
Summary:        Data structure that efficiently maps strings to integers

License:        Unicode-3.0
URL:            https://crates.io/crates/zerotrie
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Drop benchmark-only criterion dependency
# * Omit the first_weekday_for_region example, which would require the internal
#   icu_benchmark_macros crate
# * Restore writeable dev-dependency (version 0.6), which was path-based and was
#   therefore removed in Cargo.toml normalization.
# * Restore zerovec dev-dependency, which was path-based and was therefore
#   removed in Cargo.toml normalization. We have it as a (non-dev) dependency,
#   but the dev-dependency enables the serde and hashmap features.
# * Allow the previous ICU4X minor release for the icu_locale_core
#   dev-dependency; this means that all of the utility crates associated with
#   the ICU4X release can be built before any of the component crates.
Patch:          zerotrie-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A data structure that efficiently maps strings to integers.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+alloc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+alloc-devel %{_description}

This package contains library source intended for building other packages which
use the "alloc" feature of the "%{crate}" crate.

%files       -n %{name}+alloc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+databake-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+databake-devel %{_description}

This package contains library source intended for building other packages which
use the "databake" feature of the "%{crate}" crate.

%files       -n %{name}+databake-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+dense-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+dense-devel %{_description}

This package contains library source intended for building other packages which
use the "dense" feature of the "%{crate}" crate.

%files       -n %{name}+dense-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+litemap-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+litemap-devel %{_description}

This package contains library source intended for building other packages which
use the "litemap" feature of the "%{crate}" crate.

%files       -n %{name}+litemap-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+yoke-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+yoke-devel %{_description}

This package contains library source intended for building other packages which
use the "yoke" feature of the "%{crate}" crate.

%files       -n %{name}+yoke-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+zerofrom-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+zerofrom-devel %{_description}

This package contains library source intended for building other packages which
use the "zerofrom" feature of the "%{crate}" crate.

%files       -n %{name}+zerofrom-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+zerovec-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+zerovec-devel %{_description}

This package contains library source intended for building other packages which
use the "zerovec" feature of the "%{crate}" crate.

%files       -n %{name}+zerovec-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -f alloc,dense,litemap,zerovec

%build
%cargo_build -f alloc,dense,litemap,zerovec

%install
%cargo_install -f alloc,dense,litemap,zerovec

%if %{with check}
%check
%cargo_test -f alloc,dense,litemap,zerovec
%endif

%changelog
## START: Generated by rpmautospec
* Tue Sep 01 2026 Unknown User <please-configure-git-user@example.com> - 0.2.5-2
- Uncommitted changes

* Mon Aug 17 2026 Benjamin A. Beasley <code@musicinmybrain.net> - 0.2.5-1
- Update to version 0.2.5; Fixes RHBZ#2515914

* Fri Jul 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 0.2.4-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_45_Mass_Rebuild

* Sun Apr 12 2026 Benjamin A. Beasley <code@musicinmybrain.net> - 0.2.4-1
- Update to version 0.2.4; Fixes RHBZ#2453887

* Sat Jan 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 0.2.3-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_44_Mass_Rebuild

* Wed Oct 29 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.2.3-1
- Update to version 0.2.3; Fixes RHBZ#2406854

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.2.2-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Mon Jul 14 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.2.2-1
- Initial package (close RHBZ#2374316)
## END: Generated by rpmautospec
