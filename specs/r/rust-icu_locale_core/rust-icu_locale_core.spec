## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 4;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate icu_locale_core

Name:           rust-icu_locale_core
Version:        2.1.1
Release:        %autorelease
Summary:        API for managing Unicode Language and Locale Identifiers

License:        Unicode-3.0
URL:            https://crates.io/crates/icu_locale_core
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Restore missing dev-dependencies,
#   https://github.com/unicode-org/icu4x/issues/7196: use .cargo_vcs_info.json
#   in the published crate to find the corresponding git commit,
#   git clone https://github.com/unicode-org/icu4x.git, cd utils/${crate},
#   cargo publish --dry-run,
#   tar -xzf ../../target/package/${crate}-${version}.crate, and inspect
#   ${crate}-${version}/Cargo.toml to find the missing dev-dependencies. Note
#   that just looking at the crate’s Cargo.toml in the git checkout isn’t that
#   useful without the cargo publish --dry-run step because most or all of the
#   dev-dependencies are inherited from the workspace. Note also that we end up
#   omitting some of the dev-dependencies, as documented in the following
#   comments.
# * Drop benchmark-only criterion and iai dependencies
# * Omit the filter_langids and syntatically_canonicalize_locales examples,
#   which would require the internal icu_benchmark_macros crate
Patch:          icu_locale_core-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
API for managing Unicode Language and Locale Identifiers.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+alloc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+alloc-devel %{_description}

This package contains library source intended for building other packages which
use the "alloc" feature of the "%{crate}" crate.

%files       -n %{name}+alloc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+databake-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+databake-devel %{_description}

This package contains library source intended for building other packages which
use the "databake" feature of the "%{crate}" crate.

%files       -n %{name}+databake-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+zerovec-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+zerovec-devel %{_description}

This package contains library source intended for building other packages which
use the "zerovec" feature of the "%{crate}" crate.

%files       -n %{name}+zerovec-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -f alloc,databake,serde,zerovec

%build
%cargo_build -f alloc,databake,serde,zerovec

%install
%cargo_install -f alloc,databake,serde,zerovec

%if %{with check}
%check
# * Very many doctests have circular dependencies on the top-level icu crate,
#   enough that it would be very tedious to patch them all out.
%cargo_test -f alloc,databake,serde,zerovec -- --lib
%cargo_test -f alloc,databake,serde,zerovec -- --tests
%endif

%changelog
## START: Generated by rpmautospec
* Wed Apr 22 2026 azldev <azurelinux@microsoft.com> - 2.1.1-4
- Latest state for rust-icu_locale_core

* Sat Jan 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 2.1.1-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_44_Mass_Rebuild

* Thu Oct 30 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 2.1.1-2
- Run more tests

* Wed Oct 29 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 2.1.1-1
- Update to version 2.1.1; Fixes RHBZ#2406865

* Mon Oct 06 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 2.0.0-4
- Drop benchmark-only dev-dependency on iai

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 2.0.0-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Wed May 21 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 2.0.0-2
- Restore postcard dependency

* Sun May 18 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 2.0.0-1
- Initial package (close RHBZ#2366675)
## END: Generated by rpmautospec
