## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 4;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1

%global crate diskus

Name:           rust-diskus
Version:        0.9.0
Release:        %autorelease
Summary:        Minimal, fast alternative to 'du -sh'

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/diskus
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Migrate tests from tempdir to tempfile. The tempdir crate is no longer
#   maintained, and the project was archived.
#   https://github.com/sharkdp/diskus/pull/59 This requires the source-code
#   patch diskus-0.9.0-tempdir-tempfile.patch.
# * Replace atty dependency with std::io::IsTerminal,
#   https://github.com/sharkdp/diskus/pull/61. This requires the source-code
#   patch diskus-0.9.0-no-atty.patch.
Patch:          diskus-fix-metadata.diff
# * Source-code changes to migrate tests from tempdir to tempfile
Patch10:        diskus-0.9.0-tempdir-tempfile.patch
# * Document --directories in the man page
# * https://github.com/sharkdp/diskus/pull/60
Patch11:        https://github.com/sharkdp/diskus/pull/60.patch
# * Source-code change to drop the atty dependency
Patch12:        diskus-0.9.0-no-atty.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A minimal, fast alternative to 'du -sh'.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# MIT
# MIT OR Apache-2.0
License:        %{shrink:
    (MIT OR Apache-2.0) AND
    MIT AND
    (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT)
    }
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE-APACHE
%license LICENSE-MIT
%license LICENSE.dependencies
%doc CHANGELOG.md
%doc README.md
%{_bindir}/diskus
%{_mandir}/man1/diskus.1*

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+cli-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+cli-devel %{_description}

This package contains library source intended for building other packages which
use the "cli" feature of the "%{crate}" crate.

%files       -n %{name}+cli-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install
install -t %{buildroot}%{_mandir}/man1 -D -p -m 0644 doc/diskus.1

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Wed Apr 22 2026 azldev <azurelinux@microsoft.com> - 0.9.0-4
- Latest state for rust-diskus

* Thu Feb 12 2026 Benjamin A. Beasley <code@musicinmybrain.net> - 0.9.0-3
- Remove unnecessary atty dependency

* Sat Jan 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 0.9.0-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_44_Mass_Rebuild

* Tue Dec 16 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 0.9.0-1
- Initial package (close RHBZ#2422923)
## END: Generated by rpmautospec
