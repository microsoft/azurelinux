## START: Set by rpmautospec
## (rpmautospec version 0.8.3)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 2;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate zmij

Name:           rust-zmij
Version:        1.0.21
Release:        %autorelease
Summary:        Double-to-string conversion algorithm based on Schubfach and yy

License:        MIT
URL:            https://crates.io/crates/zmij
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * drop unnecessary opt-level dev-dependency
# * drop unused, benchmark-only criterion dev-dependency
Patch:          zmij-fix-metadata.diff
# * remove reference to opt-level crate API
Patch2:         0001-drop-unnecessary-opt-level-crate-API-calls.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A double-to-string conversion algorithm based on Schubfach and yy.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+no-panic-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+no-panic-devel %{_description}

This package contains library source intended for building other packages which
use the "no-panic" feature of the "%{crate}" crate.

%files       -n %{name}+no-panic-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Thu Apr 30 2026 Daniel McIlvaney <damcilva@microsoft.com> - 1.0.21-2
- test: add initial lock files

* Sun Feb 15 2026 Fabio Valentini <decathorpe@gmail.com> - 1.0.21-1
- Update to version 1.0.21; Fixes RHBZ#2437663

* Fri Feb 06 2026 Fabio Valentini <decathorpe@gmail.com> - 1.0.19-1
- Update to version 1.0.19; Fixes RHBZ#2428605

* Sat Jan 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 1.0.12-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_44_Mass_Rebuild

* Thu Jan 08 2026 Fabio Valentini <decathorpe@gmail.com> - 1.0.12-1
- Initial import (#2425791)
## END: Generated by rpmautospec
