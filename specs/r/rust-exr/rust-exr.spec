## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 2;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate exr

Name:           rust-exr
Version:        1.74.0
Release:        %autorelease
Summary:        Read and write OpenEXR files without any unsafe code

License:        BSD-3-Clause
URL:            https://crates.io/crates/exr
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Do not depend on bencher; it is needed only for benchmarks.
# * Avoid a circular dependency on image. Omit the tests (across_compression)
#   and examples (6_extract_mip_map_pngs, 1b_convert_exr_to_png,
#   7_crop_alpha_rgba, 7_crop_alpha_any_image, and
#   5b_extract_exr_layers_as_pngs) that would have required it.
Patch:          exr-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  dos2unix

%global _description %{expand:
Read and write OpenEXR files without any unsafe code.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.md
%doc %{crate_instdir}/CONTRIBUTORS.md
%doc %{crate_instdir}/GUIDE.md
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/releasing.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rayon-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rayon-devel %{_description}

This package contains library source intended for building other packages which
use the "rayon" feature of the "%{crate}" crate.

%files       -n %{name}+rayon-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# fix CRLF line endings in source files: https://github.com/johannesvollmer/exrs/issues/239
find . -type f \( -name '*.md' -o -name '*.rs' \) -execdir dos2unix --keepdate '{}' '+'

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%ifarch s390x
# * skip tests which require test data that is not included in published crates
# * skip a test that is an expected failure on big-endian architectures
%{cargo_test -- -- %{shrink:
    --skip compression::b44::test::border_on_multiview
    --skip image::validate_results::test_value_result::test_error
    --skip compare_compression_contents_
    --skip compare_png_to_
    --skip damaged
    --skip fuzzed
    --skip roundtrip_all_files_in_repository_x4
    --skip pxr24_expect_error_on_big_endian
}}
%else
# * skip tests which require test data that is not included in published crates
%{cargo_test -- -- %{shrink:
    --skip compression::b44::test::border_on_multiview
    --skip image::validate_results::test_value_result::test_error
    --skip compare_compression_contents_
    --skip compare_png_to_
    --skip damaged
    --skip fuzzed
    --skip roundtrip_all_files_in_repository_x4
}}
%endif
%endif

%changelog
## START: Generated by rpmautospec
* Wed Apr 22 2026 azldev <azurelinux@microsoft.com> - 1.74.0-2
- Latest state for rust-exr

* Sun Dec 28 2025 Benjamin A. Beasley <code@musicinmybrain.net> - 1.74.0-1
- Update to version 1.74.0; Fixes RHBZ#2414013

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 1.73.0-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Sun Jan 19 2025 Fedora Release Engineering <releng@fedoraproject.org> - 1.73.0-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_42_Mass_Rebuild

* Tue Nov 05 2024 Fabio Valentini <decathorpe@gmail.com> - 1.73.0-1
- Update to version 1.73.0; Fixes RHBZ#2322909

* Sun Sep 15 2024 Benjamin A. Beasley <code@musicinmybrain.net> - 1.72.0-3
- Avoid a circular dependency on image

* Sun Sep 15 2024 Benjamin A. Beasley <code@musicinmybrain.net> - 1.72.0-2
- Drop benchmark-only dev-dependency on bencher

* Sun Aug 04 2024 Fabio Valentini <decathorpe@gmail.com> - 1.72.0-1
- Initial import (#2297646)
## END: Generated by rpmautospec
