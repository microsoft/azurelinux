diff -rupN libdeflate-1.20/CMakeLists.txt libdeflate-1.20-new/CMakeLists.txt
--- libdeflate-1.20/CMakeLists.txt	2024-03-18 04:59:55.000000000 +0100
+++ libdeflate-1.20-new/CMakeLists.txt	2024-05-01 09:59:25.228095840 +0200
@@ -210,6 +210,10 @@ if(LIBDEFLATE_BUILD_SHARED_LIB)
     target_compile_options(libdeflate_shared PRIVATE ${LIB_COMPILE_OPTIONS})
     target_link_libraries(libdeflate_shared PRIVATE ${LIB_LINK_LIBRARIES})
     list(APPEND LIB_TARGETS libdeflate_shared)
+    if(MINGW)
+        get_target_property(LIBDEFLAT_SOVER libdeflate_shared SOVERSION)
+        set_target_properties(libdeflate_shared PROPERTIES SUFFIX "-${LIBDEFLAT_SOVER}${CMAKE_SHARED_LIBRARY_SUFFIX}")
+    endif()
 endif()
 
 # Install the static and/or shared library.
