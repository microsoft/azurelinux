diff -uPNr leaktest-1.3.0.orig/leaktest_test.go leaktest-1.3.0/leaktest_test.go
--- leaktest-1.3.0.orig/leaktest_test.go	2018-11-09 10:15:30.000000000 -0500
+++ leaktest-1.3.0/leaktest_test.go	2019-02-19 21:37:45.121184808 -0500
@@ -119,7 +119,7 @@
 
 		t.Run(leakyTestcase.name, func(t *testing.T) {
 			checker := &testReporter{}
-			snapshot := CheckTimeout(checker, time.Second)
+			snapshot := CheckTimeout(checker, 10*time.Second)
 			go leakyTestcase.f()
 
 			snapshot()
@@ -138,7 +138,7 @@
 // be based on time after the test finishes rather than time after the test's
 // start.
 func TestSlowTest(t *testing.T) {
-	defer CheckTimeout(t, 1000 * time.Millisecond)()
+	defer CheckTimeout(t, 1000*time.Millisecond)()
 
 	go time.Sleep(1500 * time.Millisecond)
 	time.Sleep(750 * time.Millisecond)
