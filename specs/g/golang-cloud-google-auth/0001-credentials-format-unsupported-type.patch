diff --git a/auth/credentials/filetypes.go b/auth/credentials/filetypes.go
--- a/auth/credentials/filetypes.go
+++ b/auth/credentials/filetypes.go
@@ -102,7 +102,7 @@ func newCredentials(opts *Options) (*auth.Credentials, error) {
 		projectID = f.Project
 		universeDomain = f.UniverseDomain
 	default:
-		return nil, fmt.Errorf("credentials: unsupported filetype %q", fileType)
+		return nil, fmt.Errorf("credentials: unsupported filetype %v", fileType)
 	}
 	return auth.NewCredentials(&auth.CredentialsOptions{
 		TokenProvider: auth.NewCachedTokenProvider(tp, &auth.CachedTokenProviderOptions{
