From 942e8fe68989733b9d22be9d860066bc0e6c0c1e Mon Sep 17 00:00:00 2001
From: shenxianpeng <xianpeng.shen@gmail.com>
Date: Sat, 15 Mar 2025 17:32:08 +0000
Subject: [PATCH] fix test failures

---
 tests/test_bdist_wheel.py            |  4 ++--
 tests/test_tagopt.py                 | 18 +++++++++---------
 tests/testdata/unicode.dist/setup.py |  2 +-
 3 files changed, 12 insertions(+), 12 deletions(-)

diff --git a/tests/test_bdist_wheel.py b/tests/test_bdist_wheel.py
index 651c034..2020a7e 100644
--- a/tests/test_bdist_wheel.py
+++ b/tests/test_bdist_wheel.py
@@ -54,9 +54,9 @@ def test_no_scripts(wheel_paths):
 @pytest.mark.skipif(sys.version_info < (3, 6),
                     reason='Packaging unicode file names only works reliably on Python 3.6+')
 def test_unicode_record(wheel_paths):
-    path = next(path for path in wheel_paths if 'unicode.dist' in path)
+    path = next(path for path in wheel_paths if 'unicode_dist' in path)
     with ZipFile(path) as zf:
-        record = zf.read('unicode.dist-0.1.dist-info/RECORD')
+        record = zf.read('unicode_dist-0.1.dist-info/RECORD')
 
     assert u'åäö_日本語.py'.encode('utf-8') in record
 
diff --git a/tests/test_tagopt.py b/tests/test_tagopt.py
index d40b078..77daa71 100644
--- a/tests/test_tagopt.py
+++ b/tests/test_tagopt.py
@@ -12,7 +12,7 @@ SETUP_PY = """\
 from setuptools import setup, Extension
 
 setup(
-    name="Test",
+    name="test",
     version="1.0",
     author_email="author@example.com",
     py_modules=["test"],
@@ -56,7 +56,7 @@ def test_default_tag(temp_pkg):
     assert dist_dir.check(dir=1)
     wheels = dist_dir.listdir()
     assert len(wheels) == 1
-    assert wheels[0].basename == 'Test-1.0-py%s-none-any.whl' % (sys.version_info[0],)
+    assert wheels[0].basename == 'test-1.0-py%s-none-any.whl' % (sys.version_info[0],)
     assert wheels[0].ext == '.whl'
 
 
@@ -67,7 +67,7 @@ def test_build_number(temp_pkg):
     assert dist_dir.check(dir=1)
     wheels = dist_dir.listdir()
     assert len(wheels) == 1
-    assert (wheels[0].basename == 'Test-1.0-1-py%s-none-any.whl' % (sys.version_info[0],))
+    assert (wheels[0].basename == 'test-1.0-1-py%s-none-any.whl' % (sys.version_info[0],))
     assert wheels[0].ext == '.whl'
 
 
@@ -79,7 +79,7 @@ def test_explicit_tag(temp_pkg):
     assert dist_dir.check(dir=1)
     wheels = dist_dir.listdir()
     assert len(wheels) == 1
-    assert wheels[0].basename.startswith('Test-1.0-py32-')
+    assert wheels[0].basename.startswith('test-1.0-py32-')
     assert wheels[0].ext == '.whl'
 
 
@@ -91,7 +91,7 @@ def test_universal_tag(temp_pkg):
     assert dist_dir.check(dir=1)
     wheels = dist_dir.listdir()
     assert len(wheels) == 1
-    assert wheels[0].basename.startswith('Test-1.0-py2.py3-')
+    assert wheels[0].basename.startswith('test-1.0-py2.py3-')
     assert wheels[0].ext == '.whl'
 
 
@@ -103,7 +103,7 @@ def test_universal_beats_explicit_tag(temp_pkg):
     assert dist_dir.check(dir=1)
     wheels = dist_dir.listdir()
     assert len(wheels) == 1
-    assert wheels[0].basename.startswith('Test-1.0-py2.py3-')
+    assert wheels[0].basename.startswith('test-1.0-py2.py3-')
     assert wheels[0].ext == '.whl'
 
 
@@ -116,7 +116,7 @@ def test_universal_in_setup_cfg(temp_pkg):
     assert dist_dir.check(dir=1)
     wheels = dist_dir.listdir()
     assert len(wheels) == 1
-    assert wheels[0].basename.startswith('Test-1.0-py2.py3-')
+    assert wheels[0].basename.startswith('test-1.0-py2.py3-')
     assert wheels[0].ext == '.whl'
 
 
@@ -129,7 +129,7 @@ def test_pythontag_in_setup_cfg(temp_pkg):
     assert dist_dir.check(dir=1)
     wheels = dist_dir.listdir()
     assert len(wheels) == 1
-    assert wheels[0].basename.startswith('Test-1.0-py32-')
+    assert wheels[0].basename.startswith('test-1.0-py32-')
     assert wheels[0].ext == '.whl'
 
 
@@ -142,7 +142,7 @@ def test_legacy_wheel_section_in_setup_cfg(temp_pkg):
     assert dist_dir.check(dir=1)
     wheels = dist_dir.listdir()
     assert len(wheels) == 1
-    assert wheels[0].basename.startswith('Test-1.0-py2.py3-')
+    assert wheels[0].basename.startswith('test-1.0-py2.py3-')
     assert wheels[0].ext == '.whl'
 
 
diff --git a/tests/testdata/unicode.dist/setup.py b/tests/testdata/unicode.dist/setup.py
index bb0ff60..ca9ebbd 100644
--- a/tests/testdata/unicode.dist/setup.py
+++ b/tests/testdata/unicode.dist/setup.py
@@ -1,7 +1,7 @@
 # coding: utf-8
 from setuptools import setup
 
-setup(name='unicode.dist',
+setup(name='unicode_dist',
       version='0.1',
       description=u'A testing distribution \N{SNOWMAN}',
       packages=['unicodedist']
-- 
2.52.0

