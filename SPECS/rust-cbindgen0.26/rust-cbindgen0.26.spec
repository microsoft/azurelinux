## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 5;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 26
# * tests require the cbindgen executable
%bcond_with check
%global debug_package %{nil}

# prevent executables from being installed
%global cargo_install_bin 0

%global crate cbindgen

Name:           rust-cbindgen0.26
Version:        0.26.0
Release:        %autorelease
Summary:        Tool for generating C bindings to Rust code

License:        MPL-2.0
URL:            https://crates.io/crates/cbindgen
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * drop cbindgen executable from the compat package
# * bump heck dependency from 0.4 to 0.5
# * bump serial_test dev-dependency from 0.5 to 3.1
Patch:          cbindgen-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 26
%if %{with check}
BuildRequires:  /usr/bin/cython
BuildRequires:  /usr/bin/gcc
BuildRequires:  /usr/bin/g++
%endif

%global _description %{expand:
A tool for generating C bindings to Rust code.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/contributing.md
%doc %{crate_instdir}/docs.md
%doc %{crate_instdir}/internals.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Mon Apr 06 2026 azldev <> - 0.26.0-5
- Latest state for rust-cbindgen0.26

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.26.0-4
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Sun Jan 19 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.26.0-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_42_Mass_Rebuild

* Wed Oct 09 2024 Fabio Valentini <decathorpe@gmail.com> - 0.26.0-2
- Disable tests and prevent cbindgen binary from being built

* Wed Oct 09 2024 Fabio Valentini <decathorpe@gmail.com> - 0.26.0-1
- Initial import (cbindgen 0.26 compat package)
## END: Generated by rpmautospec
