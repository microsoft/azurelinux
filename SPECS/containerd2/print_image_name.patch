From 432279c000895bef668a48f8ace24584c751b096 Mon Sep 17 00:00:00 2001
From: Nan Liu <liunan@microsoft.com>
Date: Tue, 22 Apr 2025 21:09:33 +0000
Subject: [PATCH] print image name in Unpack()

---
 client/image.go | 1 +
 1 file changed, 1 insertion(+)

diff --git a/client/image.go b/client/image.go
index 355bcba73..d1a6b7767 100644
--- a/client/image.go
+++ b/client/image.go
@@ -333,6 +333,7 @@ func (i *image) Unpack(ctx context.Context, snapshotterName string, opts ...Unpa
 	}
 
 	for _, layer := range layers {
+		println("apply layer for image: %q", i.Name())
 		unpacked, err = rootfs.ApplyLayerWithOpts(ctx, layer, chain, sn, a, config.SnapshotOpts, config.ApplyOpts)
 		if err != nil {
 			return fmt.Errorf("apply layer error for %q: %w", i.Name(), err)
-- 
2.34.1

