--- Changelog.iso-8859-1
+++ Changelog
@@ -60,7 +60,7 @@
       Currently available hooks are:
        pre_open, pre_read, post_read, pre_parse_value, post_parse_value
 
-    - applied patch by Ville Skytt‰, spelling fixes.
+    - applied patch by Ville Skytt√§, spelling fixes.
 
     - fixed rt.cpan.org#85080, more spelling fixes.
 
@@ -175,7 +175,7 @@
     - fixed bug in config loader for FileHandle objects, it
       supports now any compatible object. Hint by Ingo Schmiegel.
 
-    - applied spelling- and speedup patches by Ville Skytt‰.
+    - applied spelling- and speedup patches by Ville Skytt√§.
 
     - applied documentation patch by Jordan Macdonald.
 
@@ -514,10 +514,10 @@
           </directory>
 
      - fixed Makefile.PL: it cleans now files generated by 'make test'
-       properly. reported by: Dagfinn Ilmari MannsÂker <ilmari@ilmari.org>
+       properly. reported by: Dagfinn Ilmari Manns√•ker <ilmari@ilmari.org>
 
      - updated MANIFEST (in fact I did this some years ago the last time!)
-       also reported by: Dagfinn Ilmari MannsÂker <ilmari@ilmari.org>
+       also reported by: Dagfinn Ilmari Manns√•ker <ilmari@ilmari.org>
 
 
  2.24
@@ -1015,7 +1015,7 @@
 # perl-master-file: ../../webmin/index.pl ***
 # End: ***
 
-rangeh‰ngt, damit ich mit C-c d das debugging von jedem File aus 
+rangeh√§ngt, damit ich mit C-c d das debugging von jedem File aus 
 einschalten kann
 (siehe mein .emacs file)
 ----------------------------
