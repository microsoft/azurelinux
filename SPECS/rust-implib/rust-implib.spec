## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 2;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate implib

Name:           rust-implib
Version:        0.4.0
Release:        %autorelease
Summary:        Generate Windows import library from module definition file

License:        MIT
URL:            https://crates.io/crates/implib
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Generate Windows import library from module definition file.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

# includes a modified fork of crate(ar) = 0.9.0 with added symbol table support:
# * https://github.com/messense/implib-rs/commit/2449c6d
# * https://github.com/messense/implib-rs/issues/15
Provides:       bundled(crate(ar)) = 0.9.0

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%license %{crate_instdir}/src/ar/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+gnu-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+gnu-devel %{_description}

This package contains library source intended for building other packages which
use the "gnu" feature of the "%{crate}" crate.

%files       -n %{name}+gnu-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+msvc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+msvc-devel %{_description}

This package contains library source intended for building other packages which
use the "msvc" feature of the "%{crate}" crate.

%files       -n %{name}+msvc-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Mon Apr 06 2026 azldev <> - 0.4.0-2
- Latest state for rust-implib

* Tue Nov 25 2025 Fabio Valentini <decathorpe@gmail.com> - 0.4.0-1
- Update to version 0.4.0; Fixes RHBZ#2379686

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.3.5-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Tue Mar 11 2025 Fabio Valentini <decathorpe@gmail.com> - 0.3.5-1
- Initial import (#2350972)
## END: Generated by rpmautospec
