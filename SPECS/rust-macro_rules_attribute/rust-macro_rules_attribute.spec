## START: Set by rpmautospec
## (rpmautospec version 0.8.4)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 3;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# This spec file has been modified by azldev to include build configuration overlays.
# Do not edit manually; changes may be overwritten.

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate macro_rules_attribute

Name:           rust-macro_rules_attribute
Version:        0.2.2
Release:        %autorelease
Summary:        Use declarative macros in attribute or derive position

License:        Apache-2.0 OR MIT OR Zlib
URL:            https://crates.io/crates/macro_rules_attribute
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Use declarative macros in attribute or derive position.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%license %{crate_instdir}/LICENSE-ZLIB
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+better-docs-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+better-docs-devel %{_description}

This package contains library source intended for building other packages which
use the "better-docs" feature of the "%{crate}" crate.

%files       -n %{name}+better-docs-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+verbose-expansions-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+verbose-expansions-devel %{_description}

This package contains library source intended for building other packages which
use the "verbose-expansions" feature of the "%{crate}" crate.

%files       -n %{name}+verbose-expansions-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
## START: Generated by rpmautospec
* Mon Apr 06 2026 azldev <> - 0.2.2-3
- Latest state for rust-macro_rules_attribute

* Fri Jul 25 2025 Fedora Release Engineering <releng@fedoraproject.org> - 0.2.2-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_43_Mass_Rebuild

* Tue Jun 10 2025 Alexander F. Lent <lx@xanderlent.com> - 0.2.2-1
- Initial Import (rhbz#2358542).
## END: Generated by rpmautospec
