---
description: "Investigate a Koji build using task ID, URL, or package name"
agent: koji-query
argument-hint: Provide a Koji URL, task ID, package, or general query.
---

# Koji Query: `${input:target:task ID, URL, or package name}`

Answer the user's query about a Koji build.
