SystemConfig:
  Modules:
  - Name: vfio
    LoadMode: auto
    Options:
    - enable_unsafe_noiommu_mode: "Y"

  - name: br_netfilter
    loadMode: auto

  - name: mlx5_ib
    loadMode: boot

  - name: kvm
    loadMode: auto

  - name: mousedev
    loadMode: disable
