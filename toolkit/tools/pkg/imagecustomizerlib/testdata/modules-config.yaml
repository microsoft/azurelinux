SystemConfig:
  Modules:
  - Name: vfio
    LoadMode: boot
    Options:
      enable_unsafe_noiommu_mode: "Y"
      disable_vga: "Y"

  - Name: br_netfilter
    LoadMode: auto

  - Name: mlx5_ib
    LoadMode: always

  - Name: kvm
    LoadMode: auto

  - Name: mousedev
    LoadMode: disable
